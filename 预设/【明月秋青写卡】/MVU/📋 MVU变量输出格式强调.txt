{{addvar::template_knowledge::现在你需要帮助用户创作变量输出格式强调文件，参考<variable_output_format_emphasize>标签内的指导}}{{trim}}
<variable_output_format_emphasize>
# 变量规则创作指导

你的任务是帮用户写MVU变量的变量输出格式强调文件，它负责提醒AI输出变量更新

## 变量输出格式强调的格式

完全固定：

```yaml
---
变量输出格式强调:
  rule: The following must be inserted to the end of reply, and cannot be omitted
  format: |-
    <UpdateVariable>
    ...
    </UpdateVariable>
```

## 工作流程

原封不动地输出

```yaml
---
变量输出格式强调:
  rule: The following must be inserted to the end of reply, and cannot be omitted
  format: |-
    <UpdateVariable>
    ...
    </UpdateVariable>
```

重点：这就是你要输出给用户的完整格式！整个内容用代码块包裹，一次性完整输出！

开始协作吧！

---

## 完成后的引导

当变量输出格式强调创作完成并输出后，输出以下提醒：

哥哥，变量输出格式强调已完成！记得条目名字最好是`[mvu_update]变量输出格式强调`哦！

下一步：创建世界书条目

你需要在世界书中创建一个条目来存放这个变量输出格式强调，记得条目名字最好是`[mvu_update]变量输出格式强调`哦！

**这个条目只在最后测试角色卡时, 发现 AI 经常不输出变量更新 (即 `<UpdateVariable>` 块) 时才需要**

条目配置（重要！这是特殊配置）：
- 条目名：[mvu_update]变量输出格式强调
- 条目名：[mvu_update]变量输出格式强调
- 条目名（一定不要忘记[mvu_update]）：[mvu_update]变量输出格式强调
- 位置：D齿轮在深度
- 深度：0
- 顺序：200
- 内容：把刚才输出的变量输出格式强调复制进去

注意：
- 不是普通的"角色定义前"
- 必须选择D齿轮图标
- 必须是"在深度0"
- 顺序必须是200

配置完变量输出格式条目后：
1. 关闭当前条目"📋 MVU变量输出格式强调"
2. 开启"🔍 MVU变量输出格式强调"
3. 对我说"自查"

我会检查变量输出格式的结构是否正确
</variable_output_format_emphasize>