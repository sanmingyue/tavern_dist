{{addvar::self_check_instruction::MVU变量输出格式强调自查清单<variable_output_format_emphasize_check>}}{{trim}}
<variable_output_format_emphasize_check>
# MVU变量输出格式强调自查清单

当用户要求自查MVU变量输出格式强调时，使用此清单进行全面检查

## 检查项目

### 1. 变量输出格式强调是否与固定格式完全一致

```yaml
---
变量输出格式强调:
  rule: The following must be inserted to the end of reply, and cannot be omitted
  format: |-
    <UpdateVariable>
    ...
    </UpdateVariable>
```

### 2. 再次检查变量输出格式强调是否与固定格式完全一致

```yaml
---
变量输出格式强调:
  rule: The following must be inserted to the end of reply, and cannot be omitted
  format: |-
    <UpdateVariable>
    ...
    </UpdateVariable>
```

### 3. 最后检查变量输出格式强调是否与固定格式完全一致

```yaml
---
变量输出格式强调:
  rule: The following must be inserted to the end of reply, and cannot be omitted
  format: |-
    <UpdateVariable>
    ...
    </UpdateVariable>
```

### 4.条目设定提示检查

检查是否正确提示了世界书条目应该按如下设定：

条目配置（重要！这是特殊配置）：
- 条目名：[mvu_update]变量输出格式强调
- 条目名：[mvu_update]变量输出格式强调
- 条目名（一定不要忘记[mvu_update]）：[mvu_update]变量输出格式强调
- 位置：D齿轮在深度
- 深度：0
- 顺序：200
- 内容：把刚才输出的变量输出格式强调复制进去

## 检查输出格式

如果结构完全正确，直接输出：

```
# MVU变量输出格式强调自查报告

结构正确，没有发现问题
```

如果发现错误，按以下格式输出：

````
# MVU变量输出格式强调自查报告

发现以下结构错误：

1. ...

【修正后的YAML】
```yaml
---
变量输出格式强调:
  rule: The following must be inserted to the end of reply, and cannot be omitted
  format: |-
    <UpdateVariable>
    ...
    </UpdateVariable>
```
````

## 自查原则

1. **只检查结构错误**：YAML语法、格式规范、配置错误
2. **不检查内容丰富度**：假定用户已经完成所有需求，不提醒添加或补充内容
3. **如果正确就说正确**：不要为了检查而找问题
4. **给出理由**：说明为什么这是错误的
5. **提供修正方案**：如果有错误，直接给出修正后的代码

开始自查吧！

---

## 完成后的引导

当变量输出格式强调自查完成后，输出以下提醒：

哥哥，变量输出格式强调自查完成！记得条目名字最好是`[mvu_update]变量输出格式强调`哦！

**这个条目只在最后测试角色卡时, 发现 AI 经常不输出变量更新 (即 `<UpdateVariable>` 块) 时才需要**

现在有两个选择：

选择1：如果需要EJS功能

EJS可以实现更高级的功能，比如多阶段人设、状态栏等

如果你想了解EJS的详细功能：
- 需要开启"📋 EJS代码"条目
- 📋 EJS模板库保持常开（必须）
- 我就能看到完整的EJS知识
- 然后可以详细回答你的问题

如果确定需要EJS：
1. 关闭"🔍 MVU变量输出格式强调"
2. 关闭"📋 MVU变量输出格式强调"（如果还开着）
3. 确保"📋 EJS模板库"常开
4. 开启"📋 EJS代码创作"

打开后，我会看到完整的EJS知识，然后引导你创建EJS功能

如果你不确定是否需要EJS：
- 可以问我"EJS能做什么"
- 但我需要你先开启"📋 EJS代码"
- 📋 EJS模板库保持常开
- 我才能看到知识来详细解答

---

选择2：如果不需要EJS功能

如果不需要EJS的高级功能，那么：

下一步：创建前端状态栏

MVU变量系统需要状态栏来显示变量，这是可选的步骤！

请：
1. 关闭"🔍 MVU变量输出格式强调"
2. 关闭"📋 MVU变量输出格式强调"（如果还开着）
3. 开启"📋 MVU前端美化状态栏"

我会引导你创建HTML状态栏，实时显示角色的状态变量

---

请告诉我你的选择：
1. 需要EJS功能（请先开启"📋 EJS代码"，模板库保持常开）
2. 不需要EJS，继续创建前端状态栏
</variable_output_format_emphasize_check>