{{addvar::self_check_instruction::MVU变量结构脚本自查清单<variable_zod_schema_check>}}{{trim}}
<variable_zod_schema_check>
# MVU变量结构脚本自查清单

当用户要求自查MVU变量结构脚本时，使用此清单进行全面检查

## 检查项目

### 1. 头尾检查

变量结构脚本是否原封不动地照抄了头尾：

```js
import { registerMvuSchema } from 'https://testingcf.jsdelivr.net/gh/StageDog/tavern_resource/dist/util/mvu_zod.js';
```

```js
$(() => {
  registerMvuSchema(Schema);
})
```

### 2. javascript语法检查

- [ ] javascript语法正确（括号、逗号、引号配对）
- [ ] 数组和对象的嵌套层级正确

### 3. zod 4使用检查

- [ ] 没有使用任何 `.strict()` 或 `.passthrough()`
- [ ] 没有滥用 `.optional()`
- [ ] 没有对根变量的字段使用 `.optional()` 或 `.prefault()`
- [ ] `.prefault/catch(value | () => value)` 使用正确
- [ ] 针对复杂 `.object` 有使用 `.or(z.literal('待初始化')).prefault('待初始化')` 等手段保证变量能有效更新
- [ ] 只对必要的键使用了 `.describe()` 解释用途
- [ ] 尽量地使用了 `z.object()` 而不是 `z.array()`

### 4. 常见错误检查

- [ ] 数组、对象嵌套层级混乱
- [ ] 过分使用了 `.optional()` 或 `.prefault()`
- [ ] 错误使用了繁体字！需要把所有繁体字改为简体字！

## 检查输出格式

如果结构完全正确，直接输出：

```
# MVU变量结构脚本自查报告

结构正确，没有发现问题
```

如果发现错误，按以下格式输出：

````
# MVU变量结构脚本自查报告

发现以下结构错误：

1. javascript语法错误
   - 遗漏了括号

2. ...

【修正后的javascript】
```js
{
  ...修正后的完整javascript...
}
```
````

## 自查原则

1. **只检查结构错误**：javascript语法、格式规范、配置错误
2. **不检查内容丰富度**：假定用户已经完成所有需求，不提醒添加或补充内容
3. **如果正确就说正确**：不要为了检查而找问题
4. **给出理由**：说明为什么这是错误的
5. **提供修正方案**：如果有错误，直接给出修正后的代码

开始自查吧！

---

## 完成后的引导

当MVU变量结构脚本自查完成后，输出以下提醒：

哥哥，MVU变量结构脚本自查完成！

下一步：创作初始变量

请：
1. 关闭"🔍 MVU变量结构脚本"
2. 关闭"📋 MVU变量结构脚本"（如果还开着）
3. 开启"📋 MVU初始变量"

现在可以开始创作初始变量，告诉AI剧情开始时变量值是多少了
</variable_zod_schema_check>