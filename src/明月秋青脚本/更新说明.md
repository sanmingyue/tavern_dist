# 明月秋青脚本 B7.1 更新说明

## 本版改动 (B7.0 → B7.1)

### 动态人设反极端化重写
- 采用梦呓格式：`= 含义 | 不要理解为XX` 反极端化模式
- 删除大量示例节省 token，精简铁律为原则式描述（6条排比替代9条禁词列表）
- 禁止标签化和戏剧化解读，而非逐词列举

### 修复动态人设编辑保存失效
- 拆分 `saveEdits()` 逻辑：动态人设/NSFW 保存不再依赖大总结 delta
- 空内容保存时正确删除而非静默忽略

### 修复记忆条目重复显示
- `getFusedMemories()` 近期窗口已有旧核心的条目改为跳过而非仅降级
- 角色库不再出现同一文本标记为"核心"+ "近期"的重复

### 其他
- 保留 B7.0 的 `<|no-trans|>` + `stripThinking()` 思维链压制
- 保留 B7.0 的完整预设破限词
- dev-server 配置稳定

## 历史要点 (B7.0)
- Gemini 原生思维链 `[reasoning]` 泄露修复：`<|no-trans|>` + 完整破限文本
- `stripThinking()` 安全兜底
- dev-server 直接服务于 build-db 编译产物

## 文件结构
```
明月秋青脚本-备份-秋青-B7.1/
├── 源码/           # TypeScript 源码
├── dist/           # Webpack 编译产物 (index.js)
├── package.json    # 项目配置
└── 更新说明.md     # 本文件
```
