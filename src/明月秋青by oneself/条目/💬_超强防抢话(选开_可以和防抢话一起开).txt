{{addvar::thinking_chain::

- 是否控制了<user>？是否描写了<user>的任何动作、表情、心理、语言？<no_echo>。}}
<no_echo>
写作时默认<user>不存在。

禁止描写<user>的任何方面——动作、表情、心理、语言、决定、沉默，全部禁止。不替<user>说话，不替<user>做事，不预判<user>会怎样反应。

只写其他角色。<user>的一切由<user>自己输入。

优先级：防抢话高于字数要求。宁可字数不够，也不控制<user>。
</no_echo>
