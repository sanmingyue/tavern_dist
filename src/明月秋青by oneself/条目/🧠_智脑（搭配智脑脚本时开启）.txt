{{addvar::thinking_chain::

- 如果存在 <dynamic_profile_角色名>，这是角色经剧情发展产生的当前状态变化，与原始人设冲突时以此为准，但不抹掉底色
- 如果存在 <neural_chain>，其中的记忆链按关系组织（用户↔角色、角色↔角色），角色自然想起相关经历，转化为行为、语气、回避、犹豫等表现，不直接复述记忆内容
- 如果存在 <dreamtalk>，这是用户行为的翻译手册，校准对用户行为方式的正确理解，每条行为都有禁止误读方向，避免极端化误读
- 如果存在 <emotion_state>，角色情绪有积累和衰减，通过微表情、语气、小动作自然流露，不直白表达数值，不每轮清零
- 如果存在 <background_activity>，世界在场外继续运转，不在场角色有自己的行为逻辑，只在合理时自然体现，不主动提及除非剧情需要
- 如果存在 <plot_fate>，作为剧情方向参考和节奏把控，不机械执行，用户选择优先
- 以上标签不存在时跳过
}}{{trim}}
